# Node.js with Angular

# Build a Node.js project that uses Angular.
# Add steps that analyze code, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/javascript

pool:
  vmImage: 'ubuntu-latest'

steps:
- task: NodeTool@0
  inputs:
    versionSpec: '10.x'
  displayName: 'Install Node.js'

- script: |
    npm install
    npx ng build ngPorcelain
  displayName: 'npm install and build'

- script: echo $(Build.Repository.LocalPath) && pwd
  displayName: Print Workspace and pwd

- task: oneLuckiDevJson2Variable@1
  inputs:
    jsonFile: '$(Build.Repository.LocalPath)/dist/ng-porcelain/package.json'
    shouldPrefixVariables: true

- task: PublishPipelineArtifact@1
  inputs:
    targetPath: '$(Build.Repository.LocalPath)/dist/ng-porcelain'
    artifact: 'ng-porcelain@$(json.version)'

# - task: ArchiveFiles@2
#   inputs:
#     rootFolderOrFile: '$(Build.BinariesDirectory)'
#     includeRootFolder: true
#     archiveType: 'zip'
#     archiveFile: '$(Build.ArtifactStagingDirectory)/$(Build.BuildId).zip'
#     replaceExistingArchive: true

# - task: Npm@1
#   inputs:
#     command: 'publish'
#     workingDir: 'dist/ng-porcelain'
#     verbose: true
#     publishEndpoint: 'bradkovach @ npm'